# Reports

Published report pages for the Align site.

One file per report. Each file is served at /Reports/FILENAME.html on the live site.

To publish, run the Make scenario named align Publish to GitHub with path set to Reports/FILENAME.html.

File names: lowercase, hyphenated, no spaces.
